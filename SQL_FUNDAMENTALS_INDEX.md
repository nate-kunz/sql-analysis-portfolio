# SQL Fundamentals Index

This index maps each SQL skill area to the relevant files in this portfolio.

| SQL Fundamental | What It Demonstrates | Files |
|---|---|---|
| Database and table creation | Creating schemas, creating tables, assigning data types, setting primary/foreign keys | `sql/01_database_and_tables.sql` |
| Data loading and inserts | Manual `INSERT INTO` records and bulk CSV imports using `LOAD DATA INFILE` | `sql/02_load_data.sql` |
| Basic querying | `SELECT`, `WHERE`, `COUNT`, `DESC`, `SHOW`, filtering, and validation queries | `sql/03_filter_querying.sql` |
| Joins and relationships | `INNER JOIN`, multi-table joins, one-to-many relationships, and relationship validation | `sql/04_joins.sql` |
| Modification commands | `ALTER`, `UPDATE`, `DELETE`, and column changes | `sql/05_schema_and_data_modification.sql` |
| Views, renaming, constraints | Creating views, renaming tables/columns, dropping/recreating foreign keys | `sql/06_views_and_renames.sql` |
| Exports | Exporting query results and table contents with `INTO OUTFILE` | `sql/07_exports.sql` |
| Analytics and reporting | SQL analysis for RMA returns, sales by region, product returns, and fleet maintenance | `sql/08_analysis_reporting.sql` |
| End-to-end workflows | Larger project files that combine schema design, imports, queries, updates, reporting, and exports | `full_project_workflows/quantigration_rma_workflow.sql` `full_project_workflows/quantigration_updates_workflow.sql` |
