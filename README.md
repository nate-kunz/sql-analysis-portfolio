# sql-analysis-portfolio
Final project for DAD-220 (SNHU). Built a relational MySQL database from scratch with primary/foreign keys, bulk CSV data loading, CRUD operations, joins, schema refactoring, and analytics queries.
