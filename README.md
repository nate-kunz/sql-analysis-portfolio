# sql-analysis-portfolio
Final project for DAD-220 (SNHU). Built a relational MySQL database from scratch with primary/foreign keys, bulk CSV data loading, CRUD operations, joins, schema refactoring, and aggregate analytics queries. Structured to reflect real-world database workflow and console-based development.
